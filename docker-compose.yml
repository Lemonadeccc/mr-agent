services:
  mr-agent:
    build: .
    container_name: mr-agent
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
      - ./secrets/github-app.private-key.pem:/run/secrets/github-app-private-key.pem:ro
